#pragma once

// Gives the game's own keyboard-and-mouse profile an Arkham layout the first time it's missing
// from the saved mappings, so the options menu and the on-screen prompts show the real keys.
namespace keybinds {

void Install();

}  // namespace keybinds
